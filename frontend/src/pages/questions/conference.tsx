import type { ReactElement } from 'react';
import { Layout } from '../../components/layout/layout.tsx';

export function Conference(): ReactElement {
    return (
        <Layout>
            <div></div>
        </Layout>
    );
}
